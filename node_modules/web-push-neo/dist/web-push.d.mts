import { UrgencyType } from "./constants.mjs";

//#region src/web-push.d.ts
interface PushSubscription {
  endpoint: string;
  keys: {
    p256dh: string;
    auth: string;
  };
}
interface VapidDetails {
  subject: string;
  publicKey: string;
  privateKey: string;
}
interface SendNotificationOptions {
  TTL?: number;
  headers?: Record<string, string>;
  urgency?: UrgencyType;
  topic?: string;
  vapidDetails?: VapidDetails;
  signal?: AbortSignal;
}
interface SendResult {
  statusCode: number;
  headers: Headers;
  body: string;
}
interface RequestDetails {
  endpoint: string;
  method: string;
  headers: Record<string, string>;
  body: Uint8Array<ArrayBuffer> | null;
}
declare function generateRequestDetails(subscription: PushSubscription, payload?: string | Uint8Array<ArrayBuffer> | null, options?: SendNotificationOptions): Promise<RequestDetails>;
declare function sendNotification(subscription: PushSubscription, payload?: string | Uint8Array<ArrayBuffer> | null, options?: SendNotificationOptions): Promise<SendResult>;
//#endregion
export { PushSubscription, RequestDetails, SendNotificationOptions, SendResult, VapidDetails, generateRequestDetails, sendNotification };
//# sourceMappingURL=web-push.d.mts.map